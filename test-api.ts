import { POST } from './src/app/api/admin/imports/bulk/route'; async function test() { try { const req = new Request('http://localhost:3000/api/admin/imports/bulk', { method: 'POST', body: JSON.stringify({ page: 1, startDate: '2022-01-01', endDate: '2026-08-22' }) }); const res = await POST(req); console.log('Status:', res.status); console.log('Body:', await res.json()); } catch (e) { console.error('Crash:', e); } } test();
